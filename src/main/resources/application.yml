spring:
  application:
    name: arquiteturaSpring
  datasource:
    url: jdbc:h2:mem:montadora
    username: sa
    password: senha
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2-console